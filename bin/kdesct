#!/usr/bin/env bash

source "$(dirname $0)/validate-kafka-env"

if [ "${kafka_home:-}" ]; then
   cd "$kafka_home"
   if [ $# -eq 1 ]; then
      topic=$1
   else
      topic=test
   fi
   ./bin/kafka-topics.sh --describe --topic "$topic" --zookeeper "$ZK_HOSTS"
fi

# vim: ft=sh
